name: Build/release

on: push

jobs:
  release:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        include:
          # - os: macos-latest
          #   ext: dmg
          - os: ubuntu-latest
            ext: AppImage
          # - os: windows-latest
          #   ext: exe

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v1

      - name: Install Node.js, NPM and Yarn
        uses: actions/setup-node@v1
        with:
          node-version: 14

      - name: Build/release Electron app
        uses: samuelmeuli/action-electron-builder@v1
        with:
          github_token: ${{ secrets.github_token }}

          release: false
      - name: Tree
        run: |
          pwd
          ls -la
          find .
      - uses: shallwefootball/s3-upload-action@master
        name: Upload S3
        id: S3
        with:
          aws_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_bucket: ${{ secrets.S3_BUCKET }}
          destination_dir: 'launcher/${{ github.head_ref }}'
          source_dir: 'dist/decentraland-launcher.${{ matrix.ext }}'
      # - name: Archive production artifacts
      #   uses: actions/upload-artifact@v2
      #   with:
      #     name: dist
      #     path: |
      #       dist/explorer-desktop-launcher*.dmg
      #       dist/explorer-desktop-launcher*.AppImage
      #       dist/explorer-desktop-launcher*.exe